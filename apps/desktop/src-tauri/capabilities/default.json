{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-default",
  "description": "Minimum core permissions for the main ClusterYourCodex window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
